# ============================================================================
# 절대 커밋하면 안 되는 것 — 실제 API 키
# ============================================================================
# Scopus / OpenAI / Anthropic / Gmail 앱 비밀번호가 들어 있다.
# 한 번 커밋되면 히스토리에서 지우기가 매우 번거롭고, 그 사이 원격에 올라갔다면
# 키를 전부 재발급해야 한다. 형식만 공유하려면 .env.example 을 쓴다.
.env
.env.*
!.env.example

# ============================================================================
# 데이터베이스 — 서버와 로컬이 각자 소유한다
# ============================================================================
# DB 를 git 으로 주고받으면 운영 데이터를 덮어써 유실시킨다.
# 스키마 변경은 migrations/ 스크립트로만 전달한다.
instance/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# 마이그레이션이 뜨는 백업
backup/

# ============================================================================
# 환경 / 빌드 산출물
# ============================================================================
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# ============================================================================
# 에디터 / OS
# ============================================================================
.idea/
.vscode/
*.swp
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# ============================================================================
# 로컬 산출물 · 데이터 파일
# ============================================================================
# 엑셀은 코드가 아니라 데이터다. author_id.xlsx 에는 연구원 이름·Scopus ID 가
# 들어 있고 참조하는 코드도 없어 저장소에 두지 않는다. 필요하면 개별 전달.
*.xlsx
*.xls
*.csv
*.log
